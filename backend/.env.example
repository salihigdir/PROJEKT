# PostgreSQL connection settings
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password_here
PGDATABASE=projekt_db

# Optional: if you want to use a different local host binding
# PORT=3000

# Run PULS check every 15 minutes while the server is running
ENABLE_SCHEDULER=true
